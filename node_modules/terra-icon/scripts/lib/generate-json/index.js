"use strict";

var _fs = _interopRequireDefault(require("fs"));

var _parseCsv = _interopRequireDefault(require("../icon-utils/parseCsv"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

(0, _parseCsv.default)().then(function (jsonObj) {
  /* eslint-disable no-console */
  _fs.default.writeFile('src/icon-data.json', JSON.stringify(jsonObj), 'utf8', function (err) {
    if (err) {
      return console.log(err);
    }

    return console.log('icon-data.json file created in terra-icon/src.');
  });
  /* eslint-enable no-console */

});